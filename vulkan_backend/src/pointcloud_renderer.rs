use vulkano::swapchain::SwapchainAcquireFuture;

use crate::TestRenderer;

impl<const NUM_PASSES: usize> TestRenderer<NUM_PASSES> {
    fn setup_pointcloud(&mut self) {
        todo!()
    }

    fn pointcloud_renderpass(&mut self, swapchain_future: SwapchainAcquireFuture) {
        todo!()
    }
}
